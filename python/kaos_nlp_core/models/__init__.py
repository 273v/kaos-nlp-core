"""Bundled model files for kaos-nlp-core.

This subpackage exists so that ``importlib.resources.files("kaos_nlp_core.models")``
can locate model data both in editable (source-tree) installs and in wheel installs
where files are embedded in the package.
"""
