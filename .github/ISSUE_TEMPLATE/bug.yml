name: Bug report
description: Report a defect in kaos-nlp-core
title: "[bug] "
labels: ["type:bug", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for taking the time to file a bug report. Please fill out the sections below.

  - type: textarea
    id: what-happened
    attributes:
      label: What happened?
      description: A clear description of the bug. Include any error messages or stack traces.
      placeholder: When I call X with Y, Z happens instead of W.
    validations:
      required: true

  - type: textarea
    id: reproduce
    attributes:
      label: How to reproduce
      description: Minimal code or commands that reproduce the bug.
      render: python
    validations:
      required: true

  - type: input
    id: version
    attributes:
      label: kaos-nlp-core version
      description: Output of `python -c "import kaos_nlp_core; print(kaos_nlp_core.__version__)"`
      placeholder: e.g. 0.1.0a1
    validations:
      required: true

  - type: input
    id: python-version
    attributes:
      label: Python version
      description: Output of `python --version`
      placeholder: e.g. 3.13.1
    validations:
      required: true

  - type: input
    id: os
    attributes:
      label: Operating system + architecture
      placeholder: e.g. Ubuntu 24.04 x86_64, macOS 14 arm64, Windows 11 x86_64
    validations:
      required: true

  - type: textarea
    id: extra
    attributes:
      label: Additional context
      description: Anything else that might help. For Rust-side issues, include the wheel platform tag (e.g. `manylinux_2_28_x86_64`).
